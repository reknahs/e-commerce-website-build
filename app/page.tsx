import { ProductCatalog } from "@/components/product-catalog"

export default function HomePage() {
  return <ProductCatalog />
}

// Add validation to prevent negative or zero quantities in the cart component
}
